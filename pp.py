import requests

url ='http://www.google.fr"

values={(token), valeurs à poster}

r=requests.post(url, data=values)
print(r.content)

r=request.get('insta')
data = r.json()
owner = data['gsazpo']['azeaz']